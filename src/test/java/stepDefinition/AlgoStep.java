package stepDefinition;

import static org.testng.Assert.assertTrue;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;

public class AlgoStep {
	
	
	

	/*public WebDriver driver;
	@Given("The user opens Home Page")
    public void givenUserIsOnLoginPage() {
        driver.get("https://dsportalapp.herokuapp.com/"); // Replace with your actual login page URL
    }
	
	@When("The user clicks Data Structures drop down")
    public void whenUserEntersValidCredentials() {
        // Replace these values with your actual username and password
        String username = "winterchamps";
        String password = "Testwinter1!";

        WebElement usernameField = driver.findElement(By.id("id_username")); // Replace with your actual username field locator
        WebElement passwordField = driver.findElement(By.id("id_password")); // Replace with your actual password field locator

        usernameField.sendKeys(username);
        passwordField.sendKeys(password);

        WebElement loginButton = driver.findElement(By.cssSelector("input[value='login']"));// Replace with your actual login button locator
        loginButton.click();
}
	 @Then("the user should be logged in")
	    public void thenUserShouldBeLoggedIn() {
	        // Replace with your actual success message or any element that indicates successful login
	        WebElement successMessage = driver.findElement(By.xpath("//div[@class='alert alert-primary']"));

	        // Assuming successful login message is displayed
	        assertTrue(successMessage.isDisplayed());
	        System.out.println("Success Message:"+successMessage.getText());
	        // Close the browser after the test
	        driver.quit();
	    }
	*/
	
}
